import 'package:flutter/material.dart';

class CalculateEvCost extends StatelessWidget {
  const CalculateEvCost({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Calculate',
        style: TextStyle(color: Colors.white, fontSize: 24),
      ),
    );
  }
}
